<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lifeplan.mapper.TaskCategoryMapper">

    <resultMap id="BaseResultMap" type="com.lifeplan.entity.TaskCategory">
        <id column="id" property="id"/>
        <result column="dict_key" property="dictKey"/>
        <result column="dict_value" property="dictValue"/>
        <result column="dict_type" property="dictType"/>
        <result column="sort_order" property="sortOrder"/>
        <result column="status" property="status"/>
        <result column="description" property="description"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, dict_key, dict_value, dict_type, sort_order, status, description, created_at, updated_at
    </sql>

    <select id="selectByDictType" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM task_category
        WHERE dict_type = #{dictType}
          AND status = 1
        ORDER BY sort_order ASC
    </select>

    <select id="selectByTypeAndKey" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM task_category
        WHERE dict_type = #{dictType}
          AND dict_key = #{dictKey}
          AND status = 1
    </select>

</mapper>