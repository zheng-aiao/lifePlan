# 后端设计文档

## 1. 数据库表设计

### 1.1 任务主表 (task)

存储所有类型的任务基本信息

```sql
CREATE TABLE task (
    id BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '任务ID',
    title VARCHAR(200) NOT NULL COMMENT '任务标题',
    description TEXT COMMENT '任务描述',
    category VARCHAR(50) COMMENT '任务分类：工作/学习/健身/生活等',
    task_type TINYINT NOT NULL COMMENT '任务类型：1-年度任务 2-月度任务 3-日任务 4-延时任务',
    task_status TINYINT DEFAULT 0 COMMENT '任务状态：0-待开始 1-进行中 2-暂停中 3-已完成 4-未完成',
    task_group VARCHAR(50) DEFAULT '' COMMENT '任务组别',
    task_priority TINYINT DEFAULT 1 COMMENT '优先级：1-低 2-中 3-高',
    task_progress INT DEFAULT 0 COMMENT '进度百分比 0-100',
    sub_task_group VARCHAR(50) DEFAULT '' COMMENT '子任务组别',

    -- 时间相关
    planned_start_time DATETIME COMMENT '计划开始时间',
    planned_end_time DATETIME COMMENT '计划结束时间',
    actual_start_time DATETIME COMMENT '实际开始时间',
    actual_end_time DATETIME COMMENT '实际结束时间',
    actual_duration INT COMMENT '实际用时（分钟）',

    -- 关联关系
    parent_id BIGINT COMMENT '父任务ID，用于任务层级',
    user_id BIGINT NOT NULL COMMENT '所属用户ID',

    -- 元数据
    is_deleted TINYINT DEFAULT 0 COMMENT '是否删除：0-否 1-是',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',

    INDEX idx_user_type (user_id, task_type),
    INDEX idx_status (task_status),
    INDEX idx_time (planned_start_time, planned_end_time),
    INDEX idx_parent (parent_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='任务主表';
```

### 1.2 子任务表 (sub_task)

存储任务的子任务清单

```sql
CREATE TABLE sub_task (
    id BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '子任务ID',
    sub_task_group VARCHAR(50) DEFAULT '' COMMENT '子任务组别，用于关联任务',
    title VARCHAR(200) NOT NULL COMMENT '子任务标题',
    status TINYINT DEFAULT 0 COMMENT '状态：0-未完成 1-已完成',
    sort_order INT DEFAULT 0 COMMENT '排序顺序',
    finish_time DATETIME COMMENT '实际完成时间',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,

    INDEX idx_sub_task_group (sub_task_group)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='子任务表';
```

### 1.3 用户表 (user)

存储用户基本信息

```sql
CREATE TABLE user (
    id BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '用户ID',
    username VARCHAR(50) NOT NULL UNIQUE COMMENT '用户名',
    email VARCHAR(100) UNIQUE COMMENT '邮箱',
    phone VARCHAR(20) UNIQUE COMMENT '手机号',
    password_hash VARCHAR(255) NOT NULL COMMENT '密码哈希',
    nickname VARCHAR(50) COMMENT '昵称',
    avatar_url VARCHAR(500) COMMENT '头像URL',
    status TINYINT DEFAULT 1 COMMENT '状态：0-禁用 1-启用',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,

    INDEX idx_username (username),
    INDEX idx_email (email)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户表';
```

<br />

### 1.4 任务状态变更表 (task_status_change)

存储任务状态变更的反馈信息，包括完成反馈、中断原因、放弃原因和延时原因

```sql
CREATE TABLE task_status_change (
    id BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '变更记录ID',
    task_id BIGINT NOT NULL COMMENT '所属任务ID',
    change_type TINYINT NOT NULL COMMENT '变更类型：1-开始 2-暂停 3-恢复 4-完成 5-放弃 6-延时完成',
    feedback_content TEXT COMMENT '反馈内容/原因',
    duration INT COMMENT '持续时间（分钟）',

    -- 关联关系
    user_id BIGINT NOT NULL COMMENT '操作用户ID',

    -- 元数据
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',

    FOREIGN KEY (task_id) REFERENCES task(id) ON DELETE CASCADE,
    FOREIGN KEY (user_id) REFERENCES user(id) ON DELETE CASCADE,
    INDEX idx_task_type (task_id, change_type),
    INDEX idx_user (user_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='任务状态变更表';
```

<br />

<br />

## 2. 核心业务逻辑

### 2.1 任务状态流转

```
待开始 -> 进行中 -> 已完成
   |         |
   v         v
已延迟 <- 暂停
```

### 2.2 关键业务规则

1. **时间冲突检测**
   - 同一用户同一时间只能有一个进行中的任务
   - 新建/调整任务时间时需要检测是否与其他任务冲突
2. **任务自动完成**
   - 当所有子任务完成时，主任务自动标记为完成
   - 实际用时根据活动日志自动计算
3. **任务中断规则**
   - 严格准许日任务日结要求，每日0点对上日任务进行检查处理
   - 任务中断时必须记录中断原因
   - 中断时间超过当天23:59:59的任务，基于该任务状态创建新的"已延迟"任务。

## 3. 功能设计

### 3.1 日任务

1. 中断任务的处理
   - 中断状态任务包括两种： 手动点击中断按钮，过了当日 23:59:59 点，在计划时间内未完成所有子项的任务。
   - 这些当日任务记录要留存，同时基于当前任务状态负责一份新的到任务记录，切换新创建状态为未分配任务，调整优先级为最高。
   - 新复制的任务的任务组唯一标识，子任务组唯一标识 要始终与被复制任务的保持一致
   - 当某天选择再去做这个任务时，任务切换为待开始状态。系统会根据子任务组唯一标识，查找相关子任务项

## 3. API 接口设计

### 3.1 日任务管理接口

#### 3.1.1 创建任务

**接口路径**: `POST /api/tasks`

**请求参数**:

```json
{
  "title": "string (必填，最大200字符)",
  "description": "string (可选)",
  "category": "string (可选，工作/学习/健身/生活等)",
  "task_type": "integer (必填，1-年度任务 2-月度任务 3-日任务 4-延时任务)",
  "task_group": "string (可选，任务组别)",
  "task_priority": "integer (可选，默认1，1-低 2-中 3-高)",
  "sub_task_group": "string (可选，子任务组别)",
  "planned_start_time": "datetime (可选)",
  "planned_end_time": "datetime (可选)",
  "parent_id": "integer (可选，父任务ID)"
}
```

**响应格式**:

```json
{
  "code": 200,
  "message": "创建成功",
  "data": {
    "task_id": 123,
    "title": "完成项目报告",
    "task_status": 0,
    "created_at": "2026-03-31 10:00:00"
  }
}
```

#### 3.1.2 获取某日任务列表

**接口路径**: `GET /api/tasks/{date}`

**请求参数**:

- `task_type`: integer (可选，任务类型筛选)
- `task_status`: integer (可选，任务状态筛选)
- `category`: string (可选，分类筛选)

**响应格式**:

```json
{
  "code": 200,
  "message": "获取成功",
  "data": [
    {
      "id": 123,
      "title": "完成项目报告",
      "description": "撰写Q1季度项目总结报告",
      "category": "工作",
      "task_type": 3,
      "task_status": 1,
      "task_priority": 3,
      "task_progress": 60,
      "planned_start_time": "2026-03-31 09:00:00",
      "planned_end_time": "2026-03-31 18:00:00",
      "actual_start_time": "2026-03-31 09:30:00",
      "actual_end_time": "2026-03-31 18:30:00",
      "actual_duration": 120,
      "sub_task_count": 5,
      "completed_sub_task_count": 3,
      "sub_tasks": [
        {
          "id": 1,
          "title": "收集数据",
          "status": 1,
          "sort_order": 1,
          "finish_time": "2026-03-31 10:00:00"
        }
      ],
      "created_at": "2026-03-30 10:00:00"
    }
  ]
}
```

#### 3.1.3 更新任务

**接口路径**: `PUT /api/tasks/{task_id}`

**路径参数**:

- `task_id`: integer (必填，任务ID)

**请求参数**:

```json
{
  "title": "string (可选)",
  "description": "string (可选)",
  "category": "string (可选)",
  "task_priority": "integer (可选)",
  "task_progress": "integer (可选，0-100)",
  "planned_start_time": "datetime (可选)",
  "planned_end_time": "datetime (可选)"
}
```

**响应格式**:

```json
{
  "code": 200,
  "message": "更新成功",
  "data": {
    "task_id": 123,
    "title": "完成项目报告",
    "description": "撰写Q1季度项目总结报告",
    "category": "工作",
    "task_status": 2,
    "task_priority": 3,
    "task_progress": 60,
    "planned_start_time": "2026-03-31 09:00:00",
    "planned_end_time": "2026-03-31 18:00:00",
    "updated_at": "2026-03-31 12:00:00"
  }
}
```

#### 3.1.4 删除任务

**接口路径**: `DELETE /api/tasks/{task_id}`

**路径参数**:

- `task_id`: integer (必填，任务ID)

**响应格式**:

```json
{
  "code": 200,
  "message": "删除成功"
}
```

#### 3.1.6 开始任务

**接口路径**: `POST /api/tasks/{task_id}/start`

**路径参数**:

- `task_id`: integer (必填，任务ID)

**请求参数**:

```json
{
  "actual_start_time": "datetime (可选，默认当前时间)"
}
```

**响应格式**:

```json
{
  "code": 200,
  "message": "任务已开始",
  "data": {
    "task_id": 123,
    "task_status": 1,
    "actual_start_time": "2026-03-31 09:30:00"
  }
}
```

#### 3.1.7 暂停任务

**接口路径**: `POST /api/tasks/{task_id}/pause`

**路径参数**:

- `task_id`: integer (必填，任务ID)

**请求参数**:

```json
{
  "reason": "string (必填，暂停原因)"
}
```

**响应格式**:

```json
{
  "code": 200,
  "message": "任务已暂停",
  "data": {
    "task_id": 123,
    "task_status": 2,
    "duration": 120
  }
}
```

#### 3.1.8 恢复任务

**接口路径**: `POST /api/tasks/{task_id}/resume`

**路径参数**:

- `task_id`: integer (必填，任务ID)

**响应格式**:

```json
{
  "code": 200,
  "message": "任务已恢复",
  "data": {
    "task_id": 123,
    "task_status": 1
  }
}
```

#### 3.1.9 完成任务

**接口路径**: `POST /api/tasks/{task_id}/complete`

**路径参数**:

- `task_id`: integer (必填，任务ID)

**请求参数**:

```json
{
  "feedback": "string (可选，完成反馈)",
  "actual_end_time": "datetime (可选，默认当前时间)"
}
```

**响应格式**:

```json
{
  "code": 200,
  "message": "任务已完成",
  "data": {
    "task_id": 123,
    "task_status": 3,
    "actual_end_time": "2026-03-31 18:00:00",
    "actual_duration": 480
  }
}
```

#### 3.1.10 放弃任务

**接口路径**: `POST /api/tasks/{task_id}/abandon`

**路径参数**:

- `task_id`: integer (必填，任务ID)

**请求参数**:

```json
{
  "reason": "string (必填，放弃原因)"
}
```

**响应格式**:

```json
{
  "code": 200,
  "message": "任务已放弃",
  "data": {
    "task_id": 123,
    "task_status": 4
  }
}
```

#### 3.1.11 延时任务

**接口路径**: `POST /api/tasks/{task_id}/delay`

**路径参数**:

- `task_id`: integer (必填，任务ID)

**请求参数**:

```json
{
  "reason": "string (必填，延时原因)"
}
```

**响应格式**:

```json
{
  "code": 200,
  "message": "任务已延时",
  "data": {
    "task_id": 123,
    "task_status": 4
  }
}
```

### 3.2 子任务管理接口

#### 3.2.1 创建子任务

**接口路径**: `POST /api/sub-tasks`

**请求参数**:

```json
{
  "sub_task_group": "string (必填，子任务组别)",
  "title": "string (必填，最大200字符)",
  "sort_order": "integer (可选，默认0)"
}
```

**响应格式**:

```json
{
  "code": 200,
  "message": "创建成功",
  "data": {
    "id": 456,
    "sub_task_group": "sub_group_001",
    "title": "收集数据",
    "status": 0,
    "sort_order": 1,
    "created_at": "2026-03-31 10:00:00"
  }
}
```

#### 3.2.2 删除子任务

**接口路径**: `DELETE /api/sub-tasks/{sub_task_id}`

**路径参数**:

- `sub_task_id`: integer (必填，子任务ID)

**响应格式**:

```json
{
  "code": 200,
  "message": "删除成功"
}
```

#### 3.2.3 更新子任务

**接口路径**: `PUT /api/sub-tasks/{sub_task_id}`

**路径参数**:

- `sub_task_id`: integer (必填，子任务ID)

**请求参数**:

```json
{
  "title": "string (可选)",
  "sort_order": "integer (可选)",
  "status": "integer (可选)"
}
```

**响应格式**:

```json
{
  "code": 200,
  "message": "更新成功",
  "data": {
    "sub_task_id": 456,
    "title": "分析数据",
    "sort_order": 2,
    "status": 0,
    "finish_time": "2026-03-31 10:00:00",
    "updated_at": "2026-03-31 11:00:00"
  }
}
```

### 3.3 活动日志接口

#### 3.3.1 获取任务状态变更记录列表

**接口路径**: `GET /api/task-changes/{task_id}`

**路径参数**:

- `task_id`: integer (必填，任务ID)

**响应格式**:

```json
{
  "code": 200,
  "message": "获取成功",
  "data": {
    "task_id": 123,
    "task_title": "完成项目报告",
    "timeline": [
      {
        "change_type": 1,
        "change_type_name": "开始",
        "feedback_content": "开始处理项目报告",
        "duration": null,
        "created_at": "2026-03-31 09:30:00"
      },
      {
        "change_type": 2,
        "change_type_name": "暂停",
        "feedback_content": "需要等待数据确认",
        "duration": 60,
        "created_at": "2026-03-31 10:30:00"
      },
      {
        "change_type": 3,
        "change_type_name": "恢复",
        "feedback_content": "数据已确认，继续处理",
        "duration": null,
        "created_at": "2026-03-31 11:30:00"
      },
      {
        "change_type": 4,
        "change_type_name": "完成",
        "feedback_content": "项目报告已完成",
        "duration": 480,
        "created_at": "2026-03-31 18:00:00"
      }
    ]
  }
}
```

#### 3.3.2 创建状态变更记录

**接口路径**: `POST /api/task-changes`

**请求参数**:

```json
{
  "task_id": "integer (必填，任务ID)",
  "change_type": "integer (必填，变更类型：1-开始 2-暂停 3-恢复 4-完成 5-放弃 6-延迟)",
  "feedback_content": "string (可选，反馈内容/原因)"
}
```

**响应格式**:

```json
{
  "code": 200,
  "message": "创建成功",
  "data": {
    "id": 791,
    "task_id": 123,
    "change_type": 3,
    "feedback_content": "数据已确认，继续处理",
    "created_at": "2026-03-31 11:00:00"
  }
}
```

## 4. 数据同步策略

### 4.1 离线支持

- 本地存储待同步数据
- 网络恢复后自动同步
- 冲突解决策略：服务器优先 + 时间戳比对

### 4.2 实时更新

- WebSocket 连接用于实时通知
- 任务状态变更时推送消息
- 多端同步支持

## 5. 性能优化

### 5.1 索引优化

- 用户ID + 任务类型联合索引
- 时间范围索引用于查询
- 状态索引用于筛选

### 5.2 分页策略

- 默认每页20条
- 支持游标分页
- 大数据量时使用覆盖索引

### 5.3 缓存策略

- 用户任务列表缓存（Redis，5分钟）
- 热点数据本地缓存
- 缓存失效策略：写时失效
